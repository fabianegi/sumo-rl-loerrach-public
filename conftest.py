"""Root conftest.py - pytest discovers this automatically.

pythonpath is configured in pyproject.toml [tool.pytest.ini_options],
so no sys.path hacks needed here. This file serves as a marker for
pytest's rootdir detection and as a place for future shared fixtures.
"""
